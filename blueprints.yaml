name: VideoEmbed
version: 1.0.0
description: Convert links to videos into embed video.

form:
  fields:
    enabled:
      type: toggle
      label: Plugin status
      highlight: 1
      default: 0
      options:
        1: Enabled
        0: Disabled
      validate:
        type: bool
